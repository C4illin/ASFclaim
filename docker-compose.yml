version: "3"

services:
  asf-claim:
    build: .
    # environment:
      # - ASF_PORT=1242
      # - ASF_HOST=localhost
      # - ASF_PASSWORD=hunter2
      # - ASF_COMMAND_PREFIX=!
      # - ASF_HTTPS=false
